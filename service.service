[Unit]
Description=Reference nginx-gunicorn-flask API service
After=network.target

[Service]
User=USER
Group=nginx
WorkingDirectory=/home/USER/service
ExecStart=source venv/bin/activate&&gunicorn --workers 3 --bind unix:service.sock -m 007 app:app

[Install]
WantedBy=multi-user.target
